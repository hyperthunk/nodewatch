#!/usr/bin/make -f
# Makefile wrapper

all: clean compile

clean:
	@(python mk clean %(local)s %(verbose)s %(quiet)s)

compile:
	@(python mk compile %(local)s $(verbose)s %(quiet)s)

install:
	@(./install.sh)

uninstall:
	@(./uninstall.sh)

.PHONY: clean compile uninstall install all
